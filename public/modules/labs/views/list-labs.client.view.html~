<section data-ng-controller="LabsController" data-ng-init="find()">
    <div class="page-header">
        <h1>Labs</h1>
    </div>
    <div class="list-group">
        <a data-ng-repeat="lab in labs" data-ng-href="#!/labs/{{lab._id}}" class="list-group-item">
			<small class="list-group-item-text">
				Posted on
				<span data-ng-bind="lab.created | date:'medium'"></span>
				by
				<span data-ng-bind="lab.user.displayName"></span>
			</small>
			<h4 class="list-group-item-heading" data-ng-bind="lab.name"></h4> in <h4 class="list-group-item-heading" data-ng-bind="lab.building"></h4>
        </a>
    </div>
    <div class="alert alert-warning text-center" data-ng-hide="!labs.$resolved || labs.length">
    	No Labs yet, why don't you <a href="/#!/labs/create">create one</a>?
    </div>
</section>